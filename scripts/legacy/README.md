# Legacy Scripts

These are older local helper scripts from the UAV-SEAD/ALFA/threshold-calibration work.

They are kept out of the main `scripts/` view so Ibrahim can start from:

```text
../train_agent_log_detector.py
```
